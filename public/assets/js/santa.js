console.log("Happy Holidays!")

// // Call the API periodically, and obtain Santa's latest location from his helpers worldwide.
// const lotsOfCheer = setInterval(()=>{
//     fetch("/api/location/", { type: "GET" }).then((response) => {
//         return response.json();
//     }).then((response) => {
//         // Display Santa's current location, coordinates, and message on the page if there's anything he'd like us to know
//         // Can I use a grid and CSS to overlay points on a map? 
//         // Obtain the user's location from the browser
//         // If Santa is nearby, inform the user that Santa will have to come back to their home later, because they're still awake
//         console.log(response);
//     })
// }, 
// 5000)

